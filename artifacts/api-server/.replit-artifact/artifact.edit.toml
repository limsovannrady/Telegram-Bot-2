kind = "web"
previewPath = "/api"
title = "API Server"
version = "1.0.0"
id = "artifacts/api-server"
router = "path"

[[services]]
name = "api"
paths = ["/api"]
localPort = 3001

[services.development]
run = "pnpm --filter @workspace/api-server run start"

[services.env]
PORT = "3001"
